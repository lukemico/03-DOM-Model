// console.log("This is a test");

// DNA is represented by an alphabet of the following symbols: 'A', 'C', 'G', and 'T'.
// Each symbol represents a nucleotide, which is a fancy name for the particular molecules that happen to make up a large part of DNA.
// There are 5 types of nucleotides. 4 of these occur in DNA: A, C, G, and T. 4 occur in RNA: A, C, G, U.
//
//
// Find how many of each nucleotides are in a string that you pass in to a function
// Make sure that you validate it has nucleotides!

var dna = ['A', 'C', 'G', 'T'];
var rna = ['A', 'C', 'G', 'U'];

// I want to how many N_Tides are in a string

function countNuc() {
    i = 0;

    aCount = 0;
    cCount = 0;
    gCount = 0;
    tCount = 0;
    other = 0;

console.log(countNuc);
}

// document.querySelectorAll(cssQuery);
// var nucleoTides = document.querySelectorAll('ul li.catname');

// dna = document.getElementById("DNA_Sequence").value;
//     dna_length = dna.length;


nuc = {
    A: 0,
    C: 0,
    G: 0,
    T: 0,
    U: 0,
}

P nuc

















































    //
    //
    //
    // // Repeat this loop as long as the value of i is less than 3
    // while (i < dna_length) {
    //     current_letter = dna[i];
    //
    //     if (current_letter == "G") {
    //         gCount += 1;
    //     } else if (current_letter == "T") {
    //         tCount += 1;
    //     } else if (current_letter == "C") {
    //         cCount += 1;
    //     } else if (current_letter == "A") {
    //         aCount += 1;
    //     } else if (current_letter != " ") {
    //         other += 1;
    //     }
    //
    //     // update the current value of i by 1 each time
    //     i = i + 1;
    // }

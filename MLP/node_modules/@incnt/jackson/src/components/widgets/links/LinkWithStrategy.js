import React from 'react'

const LinkWithStrategy = ({href, action, strategy, children, classes, callback}) => (
  <a className={classes} href={href} onClick={(e) => { e.preventDefault(); strategy(callback)(action) }}>
    {children}
  </a>
)

export {LinkWithStrategy}
var age = 36

function calculateAge(age) {
    var yearOne = age-1;
    var yearTwo = 2017-age;
    console.log("You are either " + yearOne + " or " + yearTwo + );
};

calculateAge(25);
calculateAge(18);
calculateAge(42);
